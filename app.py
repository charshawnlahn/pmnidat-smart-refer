import streamlit as st
import google.generativeai as genai
from docx import Document
from docx.shared import Pt
import io
import re
import json

# --- 1. ตั้งค่าระบบหลังบ้าน ---
API_KEY = "AIzaSyCeLlttCAwHuTLhkqCwAxTUZ-orIMznYT8"
genai.configure(api_key=API_KEY)
model = genai.GenerativeModel('gemini-3-flash-preview')

# --- 2. ฟังก์ชันจัดการไฟล์ Word (ฟอนต์ 13 + เคาะบรรทัดอัตโนมัติ) ---
def fill_pmnidat_doc(data):
    try:
        doc = Document("PMNIDAT 062 แบบบันทึกข้อมูลเพื่อส่งต่อ.docx")
        mapping = {f"{{{{{k.upper()}}}}}": str(v) for k, v in data.items()}
        
        def apply_style_and_replace(paragraph):
            for key, value in mapping.items():
                if key in paragraph.text:
                    paragraph.text = paragraph.text.replace(key, value)
                    # บังคับฟอนต์ขนาด 13 ตามฟอร์มมาตรฐานสถาบันฯ
                    for run in paragraph.runs:
                        run.font.size = Pt(13)

        for p in doc.paragraphs: apply_style_and_replace(p)
        for table in doc.tables:
            for row in table.rows:
                for cell in row.cells:
                    for p in cell.paragraphs: apply_style_and_replace(p)
                            
        buffer = io.BytesIO()
        doc.save(buffer)
        return buffer.getvalue()
    except Exception: return None

# --- 3. การออกแบบหน้าเว็บ (Linear Workflow & Sidebar Guide) ---
st.set_page_config(page_title="PMNIDAT 062 Smart Portal", layout="wide")

# Sidebar: คู่มือสำหรับพี่พยาบาล
with st.sidebar:
    st.image("https://pmnidat.go.th/v2/wp-content/uploads/2021/01/logo-pmnidat.png", width=100)
    st.header("📖 คู่มือการสกัดข้อมูล")
    st.info("ขั้นตอนง่ายๆ: คัดลอกข้อมูลจากระบบ @ThanHIS มาวางตามช่องย่อย ดังนี้ค่ะ")
    
    st.markdown("""
    **🟢 STEP 1: ระบบผู้ป่วยใน (IPD)**
    1. **Admission Note:** คัดลอกจากหน้าแรกรับ
    2. **การวินิจฉัย:** คัดลอกหน้า ICD-10
    3. **Order/Meds:** คัดลอกจาก Discharge order
    4. **Progress Note:** คัดลอก SOAP ล่าสุด
    
    **🔵 STEP 2: ข้อมูลการประเมิน**
    * กดปุ่ม 'ข้อมูลผู้ป่วยนอก' -> เลื่อนหา **Assessment** -> คัดลอกคะแนน 9Q, 8Q, BPRS
    
    **🟠 STEP 3: เวชระเบียน (Registration)**
    * ไปที่หน้าลงทะเบียน -> ค้นหา HN -> คัดลอกข้อมูลจากหน้า 'ทั่วไป 1', 'ที่อยู่', 'ผู้ติดต่อ' และ 'สิทธิรักษา'
    """)
    st.divider()
    st.success("💡 AI จะตัดจุด ICD-10 และทำชื่อยาเป็นตัวพิมพ์ใหญ่ให้โดยอัตโนมัติค่ะ")

# หน้าจอหลัก
st.title("🏥 PMNIDAT 062 Smart Portal")
st.subheader("ระบบช่วยสรุปเวชระเบียนอัตโนมัติ (D/C Referral System)")

st.divider()

# แถวที่ 1: Step 1 (IPD)
st.markdown("### **🟢 Step 1: ข้อมูลระบบผู้ป่วยใน (IPD)**")
s1_cols = st.columns(4)
with s1_cols[0]: s11 = st.text_area("1.1 Admission Note", height=150, placeholder="วางข้อมูลแรกรับที่นี่...")
with s1_cols[1]: s12 = st.text_area("1.2 การวินิจฉัย", height=150, placeholder="วางรหัส ICD-10 ที่นี่...")
with s1_cols[2]: s13 = st.text_area("1.3 Order / Meds", height=150, placeholder="วางรายการยาและแผนจำหน่าย...")
with s1_cols[3]: s14 = st.text_area("1.4 Progress Note", height=150, placeholder="วาง SOAP บันทึกล่าสุด...")

# แถวที่ 2: Step 2 (Assessment)
st.divider()
st.markdown("### **🔵 Step 2: การประเมิน (Assessment)**")
s2 = st.text_area("คัดลอกคะแนน 9Q, 8Q, BPRS (จากหน้าข้อมูลผู้ป่วยนอก)", height=150, placeholder="วางผลคะแนนประเมินที่นี่...")

# แถวที่ 3: Step 3 (Registration)
st.divider()
st.markdown("### **🟠 Step 3: ข้อมูลเวชระเบียน (Registration)**")
s3_cols = st.columns(4)
with s3_cols[0]: s31 = st.text_area("3.1 ทั่วไป 1", height=150, placeholder="ชื่อ, อายุ, บัตร ปชช., การศึกษา")
with s3_cols[1]: s32 = st.text_area("3.2 ที่อยู่ปัจจุบัน", height=150, placeholder="คัดลอกจาก ทั่วไป 2.2")
with s3_cols[2]: s33 = st.text_area("3.3 ผู้ติดต่อ", height=150, placeholder="ชื่อญาติ, ความสัมพันธ์, เบอร์โทร")
with s3_cols[3]: s34 = st.text_area("3.4 สิทธิการรักษา", height=150, placeholder="สิทธิ์รักษา และ รพ.หลัก")

# --- 4. ส่วนประมวลผล (Advanced Synthesis Logic) ---
if st.button("🚀 สกัดข้อมูลและสังเคราะห์ใบส่งต่อ (Master V3.9)"):
    all_raw = f"{s11} {s12} {s13} {s14} {s2} {s31} {s32} {s33} {s34}"
    with st.spinner('Gemini 3 กำลังร่างเนื้อหาที่รัดกุมที่สุดเพื่อพี่พยาบาล...'):
        prompt = f"""
        จงสกัดข้อมูลเวชระเบียนลงแบบฟอร์ม 062 ตามกฎเหล็ก:
        1. ยา (MEDS): ต้องมีเลขลำดับ และเคาะบรรทัดแยกรายการ (\\n) ชื่อยาต้อง UPPERCASE พร้อมวิธีใช้ครบถ้วน
        2. วินิจฉัย (DX): เคาะบรรทัดแยกแต่ละโรค รหัส ICD-10 ติดกัน(ไม่มีจุด) + ชื่อโรคภาษาอังกฤษฉบับเต็ม
        3. สรุปปัญหา (PROGRESS): สังเคราะห์เป็น 3 ย่อหน้า: 
           (1) อาการแรกรับและความรุนแรงของโรค (2) พัฒนาการดีขึ้นอย่างไร (3) สถานะปัจจุบัน ข้อควรระวัง และแผนติดตาม
        4. กฎขยะ: Ignore ข้อมูล Theme Customizer และ Navbar ทั้งหมด
        5. ข้อมูลขาดหาย: ให้ใส่ [พิมพ์ด้วยตนเอง] ห้ามเว้นว่าง

        ข้อมูล: {all_raw}
        ตอบเป็น JSON เท่านั้น: {{"name": "..", "age": "..", "hn": "..", "id": "..", "edu": "..", "career": "..", "religion": "..", "status": "..", "rights": "..", "last_dc": "..", "admit_date": "..", "visit_num": "..", "cc": "..", "contact": "..", "relation": "..", "phone": "..", "near_hosp": "..", "dc_date": "..", "los": "..", "address": "..", "q9": "..", "q8": "..", "meds": "..", "dx": "..", "progress": "..", "post_service": ".."}}
        """
        try:
            response = model.generate_content(prompt)
            json_data = json.loads(re.search(r'\{.*\}', response.text, re.DOTALL).group())
            st.success("✅ วิเคราะห์ข้อมูลสำเร็จ!")
            
            # Preview Editor (ก่อนดาวน์โหลด)
            st.subheader("🔍 ตรวจทานข้อมูลสำคัญ (แก้ไขได้ที่นี่)")
            p1, p2 = st.columns(2)
            with p1:
                json_data['progress'] = st.text_area("สรุปปัญหา (3 ส่วน)", value=json_data.get('progress'), height=250)
            with p2:
                json_data['meds'] = st.text_area("รายการยา (ที่มีการเคาะบรรทัด)", value=json_data.get('meds'), height=110)
                json_data['dx'] = st.text_area("การวินิจฉัย (รหัส + ชื่อเต็ม)", value=json_data.get('dx'), height=110)
            
            word_file = fill_pmnidat_doc(json_data)
            if word_file:
                st.download_button("💾 ดาวน์โหลดแบบฟอร์ม 062 (ฟอนต์ 13)", word_file, f"Refer_{json_data['hn']}.docx")
        except Exception as e:
            st.error(f"ระบบขัดข้อง: {e}")
